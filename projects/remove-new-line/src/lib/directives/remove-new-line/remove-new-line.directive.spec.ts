import { RemoveNewLineDirective } from './remove-new-line.directive';

describe('RemoveNewLineDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveNewLineDirective();
    expect(directive).toBeTruthy();
  });
});
